# Release instructions

- When a version is released, create and push the Git tag.
- Create the matching GitHub release for that tag.
- Include release notes. Use generated notes when the project has no manual notes.
